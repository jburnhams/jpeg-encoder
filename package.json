{
  "name": "jpeg-encoder-wasm",
  "version": "0.1.0",
  "description": "WebAssembly JPEG encoder",
  "main": "pkg/jpeg_encoder_bg.js",
  "module": "pkg/jpeg_encoder.js",
  "types": "pkg/jpeg_encoder.d.ts",
  "files": ["pkg"],
  "type": "module",
  "scripts": {
    "build": "cargo build --manifest-path wasm-bindings/Cargo.toml --release --target wasm32-unknown-unknown && wasm-bindgen target/wasm32-unknown-unknown/release/jpeg_encoder_wasm.wasm --out-dir pkg --target bundler --typescript --out-name jpeg_encoder",
    "test": "node test/integration.test.js",
    "example": "node examples/encode.js"
  }
}
