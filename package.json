{
  "name": "jpeg-encoder-wasm",
  "version": "0.1.0",
  "description": "WebAssembly JPEG encoder",
  "main": "pkg/jpeg_encoder_bg.js",
  "module": "pkg/jpeg_encoder.js",
  "types": "pkg/jpeg_encoder.d.ts",
  "files": ["pkg"],
  "type": "module",
  "scripts": {
    "build": "wasm-pack build wasm-bindings --release --out-dir pkg && wasm-opt -Oz pkg/jpeg_encoder_bg.wasm -o pkg/jpeg_encoder_bg.wasm"
  }
}
